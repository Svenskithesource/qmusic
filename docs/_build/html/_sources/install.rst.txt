.. default-role:: code

============
Installation
============

To install QMusic API Wrapper run `pip install qmusic`. (on Windows) or `pip3 install qmusic` (on macOS and Linux). (On macOS and Linux, pip refers to Python 2’s pip tool.)

